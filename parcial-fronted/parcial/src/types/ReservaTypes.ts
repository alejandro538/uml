// Definición de Cliente
export interface Cliente {
    name: string;
    direccion: string;
    telefono: string;
    email: string;
  }
  
  // Definición de Mesa
  export interface Mesa {
    id: number;
    number: number;
    seats: number;
  }
  